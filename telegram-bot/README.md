# Telegram webhook on REG.RU

The bot runs as a PHP webhook at:

`https://imrafaelaliev.ru/telegram-bot/index.php`

Runtime secrets are generated by GitHub Actions and are not committed to the
public repository. Bot data is stored in `telegram-bot/data/storage.json` on
REG.RU and is protected from web access by `.htaccess`.
